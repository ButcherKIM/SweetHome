# 프로덕션 파이프라인

> 상태: **Draft v0.2** / 2026-09-12
> [StoryTellingSpec.md](./StoryTellingSpec.md) 의 §5 §6 §7 을 실행 가능한 수준으로 전개.
> 제작 강도 **T1 (팝업북)** 기준 — 승인 대기 항목(D5)이므로 바뀌면 이 문서도 바뀐다.

**변경 이력**
- `v0.3` **D14 = 어절** 확정 → S6에서 음절 비례 배분 단계 제거, 관련 실패 모드 해소.
- `v0.2` **유료 API 전면 배제**(D2 확정) → 이미지는 웹 UI 수작업, TTS는 edge-tts.
  S4/S5/S6 통합. **자막 하이라이트 필수화**(D3 확정) → `wordTimings` 필수, 검증 2종 추가.
- `v0.1` 최초 작성 (유료 API 전제).

---

## 0. 제약이 파이프라인에 미친 영향

| 제약 (확정) | 파이프라인 귀결 |
|---|---|
| **유료 API 불가** | 배치 자동화 불가 → **이미지는 손으로 뽑는다.** 에셋 1개 = 3~5분 |
| | → 후보 N개 생성 후 검수하는 절차가 무의미 (웹 UI는 "마음에 들 때까지 재생성") |
| | → **복붙용 프롬프트 템플릿**이 자동화 스크립트보다 중요해진다 |
| | → 캐시·멱등성 대신 **"무엇을 뽑았는지 추적"** 이 필요 |
| **한글 학습 = 핵심 기능** | `wordTimings` **필수 필드**. 없는 비트는 빌드 차단 |
| | → TTS는 **단어 타임스탬프를 주는 것만** 후보 |
| | → 자막 영역(하단 20%)을 씬 구도가 침범하지 않는지 **빌드에서 검증** |

**총 비용 목표: 0원.** 모든 도구가 무료이거나 기존 구독에 포함된다.

---

## 1. 한 장 요약

```
 ┌─ S0 기획 ───────────────────────────────────────────── 사람
 │   원작 → 13노드 트리 · 분기점 설계
 │   ⇣ outline.md
 ├─ S1 스타일 바이블 ──────────────────────── 사람 + Gemini 웹
 │   팔레트 · 질감 · 등신 · 캐릭터 시트 · 프롬프트 템플릿
 │   ⇣ style-bible.md, prompts.md, refs/*.png           ★ 게이트 1
 ├─ S2 스크립트 ────────────────────── Gemini/Claude 웹 or 수기
 │   노드 → 비트 분할 · 나레이션 · 씬 지시(enum)
 │   ⇣ script.json                                      ★ 게이트 2
 ├─ S3 에셋 명세 추출 ──────────────────────────────────── 자동
 │   참조 에셋 유니크 목록 + 재사용 횟수
 │   ⇣ asset_manifest.json
 │
 ├──────────────────────────────┬───────────────────────
 │                              │
 │  S4 에셋 제작 ── 사람 (웹 UI) │   S6 나레이션 ── edge-tts
 │   생성 → 다운로드 → 즉시 판단  │    음성 + 단어 타임스탬프
 │   ⇣ raw/*.png                │    ⇣ audio/, timings/
 │  S5 컷아웃·정규화 ────── 자동  │
 │   ⇣ assets/, assets.json     │            │
 └──────────────────────────────┴────────────┘
                 ↓
 ┌─ S7 빌드 & 검증 ───────────────────────────────────── 자동
 │   script + assets + timings → story.json (+ 검증 12종)
 │   ⇣ story.json
 ├─ S8 번들 ─────────────────────────────────────────── 자동
 │   WebP · Opus · manifest
 │   ⇣ dist/{storyId}/
 └─ ▶ 플레이어 (GitHub Pages)
```

**v0.1 대비**: 이미지 생성(구 S4)·컷아웃(구 S5)·검수(구 S6)가 **S4~S5로 통합**됐다.
웹 UI에서는 생성과 판단이 한자리에서 일어나기 때문이다.

---

## 2. 설계 원칙

| 원칙 | 의미 |
|---|---|
| **게이트는 명시적** | 사람 승인 없이 다음 단계로 흐르지 않는다 |
| **검증은 빌드 타임** | 런타임에 깨지면 아이가 본다. S7에서 전부 잡는다 |
| **되먹임은 2개뿐** | 에셋 리젝(S4 내부), 오디오 길이(S6→S2). 그 외 단방향 |
| **수작업 추적** | 무엇을 뽑았고 무엇이 남았는지 `asset_manifest.json`이 관리 |
| **교체 가능한 의존성** | edge-tts는 비공식 서비스다. **S6 한 단계로 격리**한다 |

---

## 3. 디렉터리 & 데이터 흐름

```
stories/{storyId}/
├── outline.md              S0   사람이 쓴다
├── script.json             S2   생성 or 수기 → 사람 검수
├── asset_manifest.json     S3   자동 추출 + 진행 상황 추적
├── raw/                    S4   웹 UI에서 받은 원본 (마젠타 배경)
├── assets/                 S5   컷아웃 + 정규화 완료
│   ├── bg/  char/  prop/  icon/
│   └── assets.json         S5   자동 생성
├── audio/                  S6   b_intro_04.opus
├── timings/                S6   b_intro_04.json
└── story.json              S7   ★ 최종 산출물

dist/{storyId}/             S8   배포 번들 (GitHub Pages)
style/
├── style-bible.md          S1
├── prompts.md              S1   ★ 복붙용 프롬프트 템플릿
└── refs/                   S1   캐릭터 시트
```

---

## 4. 스테이지 상세

### S0 — 기획 `사람`

| 입력 | 출력 |
|---|---|
| 원작 텍스트 (public domain) | `outline.md` |

**산출 내용**
- 13개 노드의 1~2문장 요약
- 분기점 2개의 위치와 3지선다
- 9개 결말의 제목 + 교훈 + 원작 여부
- 등장인물·장소 목록 → **에셋 물량 1차 추정** (수작업이므로 이게 곧 일정이다)

**체크**: 9개 결말이 전부 [Spec §3.4 톤 정책](./StoryTellingSpec.md#34-결말-정책)을 통과하는가.
여기서 걸러야 S2 재작업이 없다.

---

### S1 — 스타일 바이블 `사람 + Gemini 웹` ★게이트 1

| 입력 | 출력 |
|---|---|
| `outline.md` | `style/style-bible.md`, `style/prompts.md`, `style/refs/*.png` |

**style-bible.md 에 고정할 것**

```markdown
## 스타일 토큰 (모든 프롬프트에 그대로 앞에 붙인다)
traditional Korean hanji watercolor illustration, soft ink outlines,
muted mid-saturation palette, children's picture book, flat lighting

## 팔레트
주조: #E8D5B7 #C4956A #8B5A3C #6B8E6B #4A6741
강조: #D96C4F #F2C14E

## 캐릭터 규격
2.5~3등신 / 눈 크게 / 손발 단순화 / 외곽선 3px 상당

## 배경 규격
원근 얕게, 지평선 화면 하단 1/3, 인물 없음, 21:9
```

**`prompts.md` — 복붙용 템플릿** (수작업 환경에서 가장 중요한 산출물)

| 종류 | 템플릿 |
|---|---|
| 캐릭터 | `{스타일토큰}, {포즈 설명}, full body, standing on nothing, isolated on flat magenta #FF00FF background,` **`no shadow, no ground, no cast shadow`** |
| 소품 | `{스타일토큰}, {소품 설명}, single object, centered, isolated on flat magenta #FF00FF background, no shadow` |
| 배경 | `{스타일토큰}, {장소·시간 설명}, wide establishing shot, aspect ratio 21:9,` **`no people, no characters, no animals`** |
| 선택지 그림 | `{스타일토큰}, {행동 설명}, simple clear composition, single focus, magenta background` |

**캐릭터 시트**는 인물당 1장(정면/측면 + 기본 표정). **이후 모든 생성에 레퍼런스로 첨부한다.**

> ⚠️ **이 게이트 전에 S4를 시작하지 마라.** 스타일이 바뀌면 에셋 전량 재생성이고,
> 수작업 환경에서 그것은 잃어버린 하루다.

---

### S2 — 스크립트 `Gemini/Claude 웹 or 수기` ★게이트 2

| 입력 | 출력 |
|---|---|
| `outline.md`, `style-bible.md`, 스키마 발췌 | `script.json` |

**API가 없으므로 웹 UI에 노드 단위로 물어보고 결과를 붙여넣는다.** 13회(데모는 4회).
한 번에 전체를 요청하면 후반 노드 품질이 떨어지므로 어차피 쪼개야 한다.

**프롬프트에 반드시 포함**
1. [§4.5 움직임 어휘 enum](./StoryTellingSpec.md#45-움직임-어휘--폐쇄형-집합) 전문
   → **자유 서술이 아니라 enum에서 고르게 한다.** 출력 안정성의 핵심
2. 톤 정책 (배드엔딩 금지, 교훈 1문장)
3. 비트 길이 규칙 (6~12초 ≈ 한국어 **25~50자**)
4. **자막 규칙 — 2줄 초과 금지** (한 화면에 긴 문장 ✗)
5. 이미 확정된 에셋 ID 목록 → **재사용 유도**
   ("가능하면 아래 배경에서 고르고, 꼭 필요할 때만 새로 요청")

> 5번이 수작업 환경에서 특히 중요하다. 신규 에셋 1개 = 사람 시간 3~5분이다.

**게이트 2 — 검수 항목**
- [ ] 톤 정책 위반 없음
- [ ] 선택지 3개의 도덕적 우열이 드러나지 않음
- [ ] 선택지가 **그림으로 구별 가능한 행동**인가
- [ ] 비트 텍스트가 4~7세 어휘, 한 문장 = 한 사건
- [ ] **자막 2줄 안에 들어가는가**
- [ ] 신규 에셋 요청 개수가 감당 가능한가

---

### S3 — 에셋 명세 추출 `자동`

```bash
python tools/extract_assets.py --story {storyId}
```

씬 지시가 참조하는 에셋 ID를 **유니크하게** 모으고, 프롬프트 템플릿을 적용해
**바로 복붙할 수 있는 완성 프롬프트**까지 만들어 둔다.

```jsonc
{
  "summary": { "total": 16, "done": 9, "todo": 7,
               "backgrounds": 4, "characters": 6, "props": 3, "icons": 3,
               "reuse": { "char_woodcutter_idle": 7 } },
  "assets": [
    { "id": "char_woodcutter_surprise", "kind": "character",
      "status": "todo",
      "ref": "style/refs/woodcutter_sheet.png",
      "fullPrompt": "traditional Korean hanji watercolor illustration, … 놀라서 두 손을 든 자세, full body, standing on nothing, isolated on flat magenta #FF00FF background, no shadow, no ground, no cast shadow"
    }
  ]
}
```

**이 단계의 가치 (수작업이라 더 크다)**
- 뽑아야 할 목록과 **완성된 프롬프트**가 한 파일에 정리된다 → S4가 단순 반복 작업이 된다
- `status`로 진행 추적 (수작업은 여러 날에 걸쳐 나눠서 한다)
- **재사용 횟수**가 보인다 → 1회만 쓰이는 에셋은 기존 것으로 대체 검토
- 에셋 누락이 빌드가 아니라 여기서 잡힌다

---

### S4 — 에셋 제작 `사람 · Gemini 웹 UI`

**자동화 없음.** `asset_manifest.json`의 `todo` 항목을 순서대로 처리한다.

```
1. fullPrompt 복사 → Gemini 웹 UI 붙여넣기
2. 캐릭터 시트를 레퍼런스 이미지로 첨부
3. 결과 확인 → 마음에 안 들면 "다시" (여기서 즉시 판단)
4. 다운로드 → raw/{assetId}.png
5. manifest의 status를 done으로
```

**에셋 1개당 3~5분.** 데모 16개 ≈ **1시간**.

**즉석 판정 기준**
- [ ] 캐릭터 시트와 **같은 인물로 보이는가** ← 가장 중요
- [ ] 스타일 바이블 준수 (팔레트·선·등신)
- [ ] 배경에 마젠타가 고르게 깔렸는가 (그림자·바닥 없음)
- [ ] 배경 에셋: 인물이 없는가, 21:9인가

**실무 함정 4종**

| 빠뜨리면 | 생기는 일 |
|---|---|
| `no shadow, no ground` | 컷아웃 후 발밑에 회색 얼룩 |
| 배경에 `no people` | 배경 속 사람 + 컷아웃 캐릭터가 겹쳐 나옴 |
| 배경 21:9 | 카메라 팬 시 가장자리 노출 |
| 흰 배경 사용 (마젠타 대신) | 살색·흰옷이 같이 지워짐 |

> **Gemini 웹 UI 일일 생성 한도**가 있다. 데모 16개는 하루에 가능하나, 전편 30개는
> 며칠에 나눠도 무방하다. `status` 추적이 있어서 중단·재개가 자유롭다.

---

### S5 — 컷아웃 & 정규화 `자동`

```bash
python tools/cutout.py    --story {storyId}
python tools/normalize.py --story {storyId}
```

**컷아웃** (캐릭터·소품·아이콘만. 배경은 스킵)
1. 마젠타 키 1차 제거 (색상 거리 임계) — 깔끔하면 여기서 끝
2. `rembg` / BiRefNet 2차 (1차가 불완전한 경우)
3. 알파 가장자리 1px 디스페클 (마젠타 프린지 제거)

> 마젠타를 쓰는 이유: 살색·초록·하늘색과 색상 거리가 가장 멀어 오탐이 적다.

**정규화**
1. 트림 (투명 여백 제거)
2. **기준점 정렬** — 캐릭터/소품은 하단 중앙이 원점 (`y` = 발 닿는 지점)
3. 리사이즈 (마스터 2x, 캐릭터 전신 ≈ 1400px)
4. 배경 종횡비 21:9 ±2% 검증, 아니면 경고
5. `assets.json` 자동 생성

---

### S6 — 나레이션 `edge-tts`

```bash
python tools/tts_gen.py --story {storyId} [--only b_intro_04]
```

| 입력 | 출력 |
|---|---|
| `script.json` 의 `text`, `choice.*.label`, `promptText` | `audio/*.opus`, `timings/*.json` |

**무료이며 단어 타임스탬프를 준다.** 한글 학습이 핵심 기능이므로 후자가 채택 이유다.

**처리**
1. `edge-tts` 로 합성 (`ko-KR-SunHiNeural` 등) — API 키 불필요
2. `WordBoundary` 이벤트 수집 → 어절별 `(offset, duration)`
3. 어절 단위 `wordTimings` 산출 — **변환·근사 없이 그대로** (D14 확정)
4. 실제 `durationMs` 측정
5. Opus 인코딩 (48kbps mono)

**되먹임 — 길이 리포트**

```
⚠ b_intro_04: 14.2s (상한 12s 초과) → 비트 분할 필요
⚠ b_end1_03:   3.1s (하한 6s 미만) → 앞 비트와 병합 검토
⚠ b_mid_a_02: 자막 3줄 → 문장 축약 필요
```

리포트를 들고 S2로 돌아가 해당 비트만 다시 쓴다. **두 되먹임 루프 중 하나.**

> ⚠️ **edge-tts는 Microsoft의 비공식 엔드포인트다.** 개인 프로토타입에는 적합하나
> **상용 배포 전 교체 필수.** 그래서 이 단계를 독립 스크립트로 격리했다 —
> 교체 시 `tts_gen.py` 하나만 바뀌고 `timings/*.json` 포맷은 그대로다.
>
> **교체 후보**: 직접 녹음 + `whisperX` 강제정렬 / 유료 TTS (예산 생기면).

---

### S7 — 빌드 & 검증 `자동`

```bash
python tools/build_story.py --story {storyId}
```

| 입력 | 출력 |
|---|---|
| `script.json` + `assets.json` + `timings/` | `story.json` |

**검증 12종 — 하나라도 실패하면 빌드 실패**

| # | 검증 | 잡는 사고 |
|---|---|---|
| V1 | JSON Schema 준수 | 구조 오류 |
| V2 | 참조 무결성 — `background`/`actors.asset` 이 `assets` 에 존재 | 검은 화면 |
| V3 | 파일 실재 — 선언된 경로에 파일이 있는가 | 404 |
| V4 | **트리 무결성** — 모든 `next` 유효, 도달 불가 노드 없음, 순환 없음 | 막다른 길 |
| V5 | **결말 수** — `type:"ending"` 이 정확히 9개(v0는 3개), `isCanonical` 정확히 1개 | 원작 엔딩 누락 |
| V6 | enum 유효성 — `camera`/`anim`/`enter`/`transitionIn`/`fx` | 렌더러 무반응 |
| V7 | 길이 규칙 — `durationMs` 6000~15000, 오디오 실측과 ±100ms 일치 | 자막 밀림 |
| V8 | 타임스탬프 정합 — `wordTimings` 끝 ≤ `durationMs`, 인덱스가 `text` 범위 내 | 하이라이트 깨짐 |
| V9 | 텍스트 규칙 — 선택지 `label` 길이, `lesson` 1문장 | UI 넘침 |
| V10 | 좌표 범위 — `x`,`y` ∈ [0,1], `z` 충돌 없음 | 화면 밖 캐릭터 |
| **V11** | **`wordTimings` 존재** — 모든 비트·선택지에 필수 | **하이라이트 불가** |
| **V12** | **자막 영역 확보** — 캐릭터 `y` ≤ 0.88, 하단 20%에 핵심 요소 없음 | **자막이 그림을 가림** |

> V4·V5 는 분기형 콘텐츠의 고유 위험이다. 13노드가 되면 사람 눈으로 못 잡는다.
> **V11·V12 는 한글 학습을 핵심 기능으로 올린 데 따른 신규 검증이다.**

**CI**: `stories/**/story.json` 변경 시 V1~V12 자동 실행 (GitHub Actions, 무료).

---

### S8 — 번들 `자동`

| 자산 | 처리 | 목표 |
|---|---|---|
| 이미지 | WebP q82, 1x/2x | |
| 오디오 | Opus 48kbps mono | |
| story.json | minify | |
| **합계** | | **20MB 이하 / 편** |

`dist/{storyId}/manifest.json` 에 선행 로드 순서를 기록한다
(현재 비트 + 다음 비트 + 선택지 3갈래의 첫 비트).

배포는 **GitHub Pages** (무료).

---

## 5. 되먹임 루프 (2개만 존재)

```
 ┌─── L1: 에셋 리젝 ──────────────────────┐
 │  S4 내부에서 즉시 재생성                 │  웹 UI라 자리에서 끝난다
 └─────────────────────────────────────────┘

 ┌─── L2: 오디오 길이 / 자막 줄수 ────────┐
 │  S6 리포트 → S2 해당 비트 재작성         │  1~2회면 수렴
 └─────────────────────────────────────────┘
```

그 외는 전부 단방향이다. **스타일 바이블(게이트 1)이 루프가 되지 않게 하는 것**이
유일하게 중요한 레버다. 수작업 환경에서 전량 재생성은 잃어버린 하루다.

---

## 6. 실패 모드 대응표

| 증상 | 원인 | 대응 |
|---|---|---|
| 컷아웃 발밑에 회색 얼룩 | 프롬프트에 `no shadow` 누락 | S4 프롬프트 수정 후 재생성 |
| 캐릭터 외곽 마젠타 테두리 | 디스페클 미적용 | S5 후처리 강화 |
| 배경에 사람이 그려져 있음 | `no people` 누락 | S4 재생성 |
| 같은 캐릭터가 달라 보임 | 레퍼런스 미첨부 / 시트 품질 | S1 시트 재작업 → S4 전량 |
| 배경 팬 시 가장자리 노출 | 21:9 미준수 | S5가 경고 → S4 재생성 |
| 자막이 음성보다 빠름/느림 | 타임스탬프 스케일 오류 | V8 확인, S6 재실행 |
| **자막이 캐릭터 얼굴을 가림** | 씬 구도가 하단 20% 침범 | **V12가 빌드에서 차단** → `y` 조정 |
| 특정 결말에 도달 불가 | `choice.next` 오타 | V4가 빌드에서 차단 |
| 비트가 12초 초과 | 스크립트 과다 | L2 루프 |
| edge-tts 연결 실패 | 비공식 엔드포인트 변동 | 재시도 → 지속되면 교체 검토 |

---

## 7. 시간 · 비용 추정 (데모 = 에셋 16개 / 비트 15개)

| 스테이지 | 기계 시간 | 사람 시간 | 비용 |
|---|---|---|---|
| S0 기획 | — | **3~5h** | 0 |
| S1 스타일 바이블 | — | **4~8h** | 0 (구독) |
| S2 스크립트 | — | **2~3h** (생성+검수, 웹 UI 수작업) | 0 (구독) |
| S3 명세 추출 | 즉시 | — | 0 |
| **S4 에셋 제작** | — | **1~1.5h** (16개 × 3~5분) | 0 (구독) |
| S5 컷아웃·정규화 | 5m | — | 0 |
| S6 나레이션 | 5m | — | **0** |
| S7 빌드·검증 | 즉시 | — | 0 |
| S8 번들 | 2m | — | 0 |
| **합계** | **~15m** | **10~18h** | **0원** |

**기계는 15분, 사람은 이틀. 비용은 0원.**
최적화 대상은 비용도 기계 시간도 아니고 **게이트 1(스타일 바이블)** 이다.
여기가 흔들리면 S4가 통째로 반복된다.

**전편(에셋 30 / 비트 60) 확장 시**
- S1 은 **재실행하지 않는다** (0h) ← 캐릭터 분리 생성의 가장 큰 보상
- S4 는 추가분 14개만 = **45~70분**
- S2·S6 는 비트 수에 비례 (약 4배)
- 증가분은 대부분 S2 작성·검수

---

## 8. 구현 순서

파이프라인을 한 번에 짜지 않는다. **S7(빌드·검증)과 플레이어를 먼저** 만든다.
그래야 만든 에셋을 눈으로 확인할 수 있다.

| 순서 | 대상 | 이유 |
|---|---|---|
| 1 | `schema/story.schema.json` + **생성기 없이 만든** `story.json` 1개 | 모든 것의 계약 (아래 주) |
| 2 | 플레이어 (더미 도형 에셋) | 움직임 어휘 + **자막 하이라이트** 확정 |
| 3 | S7 `build_story.py` + V1~V12 | 검증 없이 만든 콘텐츠는 못 믿는다 |
| 4 | S6 `tts_gen.py` | **하이라이트 검증에 실제 타임스탬프가 필요하다** |
| 5 | S4·S5 (캐릭터 **1명**만 관통) | [Spec §9.3 질문 #3](./StoryTellingSpec.md#93-데모가-답해야-할-질문) |
| 6 | S3 `extract_assets.py` | 손으로 하다 지치면 |
| 7 | S2 (나머지 스크립트) | 스키마가 안정된 뒤 |
| 8 | S8 번들 | 배포 직전 |

> **1번의 "생성기 없이"는 누가 타이핑하는지가 아니라 순서를 말한다.** S2(자동 생성)를
> 짓기 전에 한 편을 끝까지 만들어보며 규격의 구멍을 찾으라는 뜻이다. 다만 완성된 샘플은
> **작성자가 아닌 사람이 한 비트를 고쳐볼 수 있어야** 한다 — 못 고치면 규격이 잘못된 것이고,
> 이것이 규격 검증의 실질적 기준이다.

**v0.1 대비 변화**: TTS가 6번에서 **4번으로 올라왔다.** 자막 하이라이트가 핵심 기능이
되면서, 더미 타임스탬프로는 [질문 #6(글자를 따라 읽으려 하는가)](./StoryTellingSpec.md#93-데모가-답해야-할-질문)
을 검증할 수 없기 때문이다.
