# 골 — 기획한 것을 다 구현했는가

> 골은 새로 정하는 것이 아니다. [StoryTellingSpec.md](./StoryTellingSpec.md) 에 이미
> 적혀 있는 것을 **다 구현했는가**가 골이다. 이 문서는 그 대조표다.
>
> `✅ 됨` · `🟡 일부` · `⬜ 아직` · `⏸ v1로 미룸(기획서가 미룬 것)`
>
> 마지막 대조: 2026-09-15 · 기획서 v0.8

## 한눈에

| 영역 | 됨 | 일부 | 아직 |
|---|---|---|---|
| 서사 — 트리·결말·톤 | 7 | 1 | 0 |
| 스키마 — story.json | 6 | 0 | 0 |
| 그림 | 3 | 0 | 1 |
| 소리 | 8 | 0 | 2 |
| 플레이어 | 18 | 1 | 0 |
| 프로덕션 | 3 | 0 | 2 |
| 실사용 확인 | 0 | 0 | 7 |
| **합계** | **45** | **2** | **12** |

**한 줄:** 이야기·소리·앱이 기획대로 서 있고, **그림 0장**이 남았다.
남은 12개 중 7개는 아이에게 보여줘야 답이 나오는 것들이다.

---

## 1. 서사 — 기획서 §3

| 기획한 것 | 상태 | 확인 |
|---|---|---|
| 노드 13개 = 도입 1 + 중간 3 + 결말 9 | ✅ | `stories/goldaxe/story.json` |
| 분기 2회 × 선택 3개 | ✅ | `tests/test_story.py::test_every_choice_has_three_options` |
| 결말 9개 중 원작 1개 | ✅ | `canonicalEnding: n_end_wade_honest` |
| 그림 한 장이 줄 2~3개를 덮는다 | ✅ | 17비트 / 38줄 — 비트당 2~3줄 |
| 줄 6~12초 · 그림 12~35초 | ✅ | 검증 V7 |
| 배드엔딩 없음 — 9개가 서로 다른 결말 | ✅ | 대본 |
| 결말마다 갈등 해소 · 따뜻한 마지막 · 교훈 한 줄 | ✅ | `endingTitle` / `lesson` |
| 선택지 3개의 도덕적 우열이 안 드러난다 | 🟡 | 대본은 지켰다. **아이에게 확인한 적 없음** (§9.3 #2) |

## 2. 스키마 — 기획서 §4

| 기획한 것 | 상태 | 확인 |
|---|---|---|
| `story.schema.json` 고정 | ✅ | schemaVersion 0.5 |
| 비트(그림) / 줄(자막) 2단 구조 | ✅ | §4.4 |
| 움직임 폐쇄형 집합 — camera 6 · anim 6 · enter 4 · transitionIn 4 · fx 7 | ✅ | 27개 값 전부 렌더러에 있음 |
| 발광체 `blend: "screen"` | ✅ | |
| 배경 후처리 `bgTreatment` (stage + 비트별) | ✅ | |
| 빌드 검증 V1~V13 | ✅ | `tools/validate_story.py` |

## 3. 그림 — 기획서 §5

| 기획한 것 | 상태 | 확인 |
|---|---|---|
| 스타일 바이블 먼저 고정 (D15 한지 수채) | ✅ | `style/style-bible.md` |
| 캐릭터를 배경에서 분리 생성 | ✅ | 프롬프트가 그 구조로 짜여 있음 |
| 마젠타 배경 → 컷아웃 자동화 | ✅ | `tools/cutout.py` |
| **에셋 22장** | ⬜ | **0 / 22.** 여기서 전부 막혀 있다 |

## 4. 소리 — 기획서 §6

| 기획한 것 | 상태 | 확인 |
|---|---|---|
| edge-tts · SunHi · `--rate=-28%` | ✅ | mp3 42개 |
| `boundary="WordBoundary"` — 어절 타임스탬프 | ✅ | `tools/tts_gen.py` |
| `durationMs` 를 합성 실측으로 덮어쓰기 | ✅ | 38줄 전부 실측값 |
| 자막 규격 — 64px · 어절 간격 · 1.08배 확대 · 반투명 박스 | ✅ | `player/prototype.html:72-82` |
| 한 시계 — 오디오 끝 = 자막 끝 = 비트 끝 | ✅ | §6.4 |
| 선택지 낭독 — 순서대로 읽어준다 | ✅ | mp3 54개 (줄 38 + 질문 4 + 선택지 12) |
| **자막 싱크 자동 보정** (`outputLatency`) | ✅ | `src/audio/latency.ts`. 실패 시 150ms, 슬라이더는 미세조정용으로 남김 |
| **"시작하기" 탭 게이트** (자동재생 정책) | ✅ | 표지 버튼에서 `resume()` + 지연 측정 |
| BGM 3종 + 나레이션 중 덕킹 | ⬜ | **CC0 음원이 없다.** `AudioBus` 에 자리와 덕킹(-18dB)은 있고 파일만 비었다 |
| SFX (선택 확정 · 페이지 넘김) + `CREDITS.md` | ⬜ | 위와 같음 |

## 5. 플레이어 — 기획서 §8

`player/` — React + Vite + TypeScript. `npm run build` 통과, 브라우저에서 끝까지 돌려 확인했다.

| 기획한 것 | 상태 | 확인 |
|---|---|---|
| **React + Vite 앱** | ✅ | `player/` — 번들 js 239KB + css 8KB |
| **엔진과 렌더러를 파일 경계로 가른다** | ✅ | `src/engine/` 은 순수 TS. `tests/test_engine_is_pure.py` 가 CI에서 강제 |
| 렌더러 구조 (SceneView / SubtitleBar / ChoiceOverlay / AudioBus …) | ✅ | §8.2 구조대로 |
| 화면 어디든 탭 = 다음 비트 | ✅ | |
| 선택 화면은 자동 진행 없음 | ✅ | |
| 한 시계 — 오디오 끝 = 자막 끝 = 비트 끝 | ✅ | `usePlayback` 하나만 넘긴다 |
| 자막 기본 ON + 어절 하이라이트 | ✅ | 1080p 환산 63.7px 실측 |
| 원작 엔딩 도달 시 "원작 엔딩을 찾았어요!" | ✅ | |
| 카메라 6 · 모션 6 · 등장 4 · 전환 4 · 파티클 7 | ✅ | `app.css` + `FxCanvas` |
| 배경 후처리 (stage + 비트별) | ✅ | |
| **표지 화면에 본 엔딩 목록** | ✅ | 9칸, 못 본 것은 `？` |
| **선택지를 순서대로 읽어준다** | ✅ | 질문 → 1 → 2 → 3 |
| **탭하면 다시 읽어준다** | ✅ | 고른 순간 그 선택지를 한 번 더 들려주고 넘어간다 |
| **선택 후 즉시 시각 피드백** | ✅ | 고른 그림 1.1배, 나머지 흐려짐 |
| **길게 누르기 게이트** (되돌리기·설정·자막토글) | ✅ | 0.8초. 짧게 누르면 안 열리는 것까지 확인 |
| 선택지 규격 — 그림 320px · 터치 120px | ✅ | 1080p 환산 459px / 508px 실측 |
| **로딩 대기 화면 · 다음 비트 선행 로드** | ✅ | 정지 화면 없음 · `preloadPaths` (현재+다음+선택지 갈래) |
| 진행 상태 저장 | ✅ | 본 엔딩·자막·싱크값. 저장 막힌 환경에서도 돈다 |
| 성능 — 60fps · 초기 3초 · 번들 20MB | 🟡 | **번들 1.8MB** 로 목표 안. 프레임·로드는 실기기에서 측정해야 한다 |

### 대조하다 찾은 것

- **엔딩 카운터가 `/ 3` 으로 박혀 있었다.** 결말이 9개가 된 뒤에도 안 고쳐져 있었다.
  `prototype.html` 에서 결말 수를 세도록 고쳤다 — 플레이어 아티팩트는 한 번 다시
  올려야 반영된다.
- **선택지를 읽어주는 동안 탭이 안 먹었다.** 앱을 처음 돌려보고 찾았다. 아이가 누른 것이
  안 먹히면 고장으로 받아들이므로, 낭독 중에도 탭하면 바로 고르도록 고쳤다.
- **선택지 낭독 음성이 아예 없었다.** 스키마에 `option.audio` 가 없었다. 필드를 넣고
  `tts_gen.py` 가 선택지도 합성하게 해서 mp3 12개를 더 만들었다.

## 6. 프로덕션 — 기획서 §7 · Pipeline.md

| 기획한 것 | 상태 | 확인 |
|---|---|---|
| 전량 프리베이크 (런타임 AI 생성 없음) | ✅ | |
| 파이프라인 S0~S8 문서화 | ✅ | `Pipeline.md` |
| 비용 0원 | ✅ | 유료 API 한 건도 없음 |
| S3 에셋 명세 자동 추출 | ⬜ | 손으로 적고 있다 |
| S8 번들 (WebP/Opus 변환, 20MB 이하) | ⬜ | |

## 7. 실사용 확인 — 기획서 §9.3

**전부 아직.** 아이에게 보여준 적이 없다. 그림이 나와야 시작할 수 있다.

| # | 질문 | 상태 |
|---|---|---|
| 1 | 4세가 끝까지 앉아 있는가 | ⬜ |
| 2 | 글 없이 선택지를 이해하는가 | ⬜ |
| 3 | 같은 캐릭터로 보이는가 | ⬜ |
| 4 | "살아있다"고 느끼는가 | ⬜ |
| 5 | 다른 결말을 보려고 다시 하는가 | ⬜ |
| 6 | 글자를 쳐다보거나 따라 읽으려 하는가 | ⬜ |
| 7 | 학부모가 학습 가치를 인정하는가 | ⬜ |

## 8. 로드맵 — 기획서 §11

| | 범위 | 상태 |
|---|---|---|
| **M0** 스키마 | schema + story.json | ✅ |
| **M1** 렌더러 | 움직임 어휘 전부 · 오디오 싱크 | 🟡 프로토타입으로는 됨 |
| **M2** 에셋 관통 | 캐릭터 1명 생성→컷아웃→화면 | ⬜ |
| **M3** 데모 | 질문 #1~5 측정 · 자막 싱크 자동 보정 | ⬜ |
| **M4** 전편 | 2분기 / 9결말 | 🟡 대본·소리는 전편, 그림 0 |
| **M5** 2편차 | 두 번째 원작 | ⬜ |
| **M6** 제품화 | React Native 전환 (D16) | ⬜ |

## 9. 아직 정하지 않은 것

기획서 §12 미결. **골에 넣을 수 없다 — 정해진 적이 없으므로.**
D16(플랫폼)은 확정됐다 — 데모까지 웹, 제품화에서 React Native (§8.5).

| | 내용 | 막는 것 |
|---|---|---|
| D17 | 화면 방향 — 가로 고정 / 양방향 | 씬 레이아웃 |
| D18 | 오프라인 지원 | 번들 전략 |
| D19 | 엔딩 수집 보상 | 재플레이 동기 |

## 10. 미해결 리스크

| | 내용 |
|---|---|
| R9 | 원작이 아닌 결말 8개의 서사 품질. 편차가 보이면 "가짜 선택"이 된다 |
| R17 | 학습 효과가 학부모에게 체감되지 않을 가능성 (질문 #7) |
| R18 | RN 전환 시 `blend: screen` · `blurPx` 가 안 되면 에셋 물량이 는다. RN 착수 전 선검증 |

---

## 그래서 다음은

1. **그림 22장** — [작업판](https://claude.ai/code/artifact/b95966cb-4e21-4f3c-a32a-52fd239f9921)
2. 컷아웃 → 프로토타입에 넣고 **질문 #3·#4** 확인
3. **웹 앱** (React + Vite) — 엔진과 렌더러를 갈라서. §5 표의 ⬜ 들이 여기서 구현된다
4. 아이에게 테스트 — 질문 #1~#7
5. 그다음이 React Native. R18 선검증부터
